db:
    image: dockerfile/mongodb
    ports:
        - 27017
web:
    build: .
    command: npm start
    volumes:
        - ./coverage:/src/coverage
        - ./test:/src/test
        - ./api:/src/api
    ports:
        - 3000:3000
    links:
        - db
    environment:
        DEBUG: raspberry-pi-io-api:*
        JWT_SECRET: fakeToken
        JWT_EXPIRES: 20160
